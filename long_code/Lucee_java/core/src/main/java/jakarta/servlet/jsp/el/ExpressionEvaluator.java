package jakarta.servlet.jsp.el;

/**
 * Compatibility shim for Jakarta EE 10 ExpressionEvaluator This interface exists to maintain API
 * compatibility when running on Jakarta 11+ where the original interface was removed.
 */
public interface ExpressionEvaluator {

}